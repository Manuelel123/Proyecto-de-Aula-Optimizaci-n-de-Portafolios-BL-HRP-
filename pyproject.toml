[project]
name = "optimizacion-portafolios"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Manuelel123", email = "bedoyayonatan164@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.9.0",
    "numpy>=2.0.0",
    "pandas>=2.0.0",
    "quantstats>=0.0.77",
    "scipy>=1.14.0",
    "statsmodels>=0.14.4",
    "streamlit>=1.64.0",
    "yfinance>=1.7.0",
]

[project.scripts]
optimizacion-portafolios = "optimizacion_portafolios:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
